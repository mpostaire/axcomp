# time in milliseconds between each effect step
effect-delta = 3

effect fade {
    function = fade
    step = 0.03
}

effect fade_slow {
    function = fade
    step = 0.01
}

effect pop {
    function = pop
}

effect slide {
    function = slide
}

effect-rules {
    wintype dock {
        map-effect = slide
        unmap-effect = slide
    }
    wintype tooltip {
        map-effect = fade
        unmap-effect = fade_slow
    }
    wintype normal {
        map-effect = pop
        unmap-effect = pop
    }
}
